<%= form_for(@item) do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>
  <%= f.check_box :complete, {class: 'browser-default'}, 'Yes', 'No' %>
  <%= f.label :complete %>
  <br>
  <%= f.submit 'Save', class: 'btn' %>
<% end %>
